# Import Bill

## 目标

支持用户导入账单，并在需要密码时以清晰方式继续完成导入。

## 主路径

1. 选择文件。
2. 先 probe，再 import。
3. 如需密码，提示补充密码。
4. 成功后刷新结果反馈。

## 约束

- UI/UX 对接需要基于主仓库当前 `probe -> import` 后端能力。
- 如果文件识别结果或密码交互需要重新定义，必须先回到 brief。
