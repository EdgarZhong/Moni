# Data Range Filtering

## 目标

让用户用最少操作切换阅读时间范围，并让首页相关信息同步更新。

## 规则

- Data Range Picker 是唯一入口。
- 标题、统计、分类概览、日卡片标题都必须联动。
- `data range` 当前主要约束首页阅读范围与 AI 消费边界参考，不直接改写底层脏日期生产。

## 异常边界

- 范围为空时要有明确空态。
- 调整中应避免手势冲突与卡顿。
