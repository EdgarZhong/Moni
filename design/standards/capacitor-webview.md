# Capacitor WebView

## 目标

约束 Moni 在 Android Capacitor 与浏览器开发替身中的表现边界。

## 规则

- 正式目标环境是 Android Capacitor。
- 浏览器 F12 移动模拟模式用于高保真开发验证，但不等价于真机验收。
- 安全区、软键盘、触觉反馈、WebView 滚动行为需作为长期约束考虑。
- 若视觉或交互依赖系统能力，需要先说明浏览器替身与真机的差异。

## 约束

- `__design` 只在开发态启用，不进入正式移动端产物。
- prototype 不接真实业务逻辑，不承担正式实现职责。
