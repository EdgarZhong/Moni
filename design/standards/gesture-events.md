# Gesture Events

## 范围

本文件约束长按、拖拽、Pointer Events、嵌套滚动与取消行为。

## 规则

- 长按、单击、拖拽阈值必须清晰，避免一条交互触发多个结果。
- 需要时使用 Pointer Capture 保证拖拽稳定跟手。
- 横向分类轨道、纵向页面滚动、卡片内滑动必须明确边界。
- Android WebView 与 F12 移动模拟模式都要验证轻微手抖、越界与取消情况。
- 控制条打开后，点击空白关闭的规则必须固定。

## 禁止事项

- 不得靠代码中的临时变通绕过设计层手势冲突。
- 若手势规则需要改变，先更新 brief 与本文件。
