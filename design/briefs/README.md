# Briefs

本目录存放 Moni 所有 UI/UX 任务的设计 brief。

- `active/`：正在讨论、尚未拍板的 brief。
- `accepted/`：已经通过审查、可作为正式实现输入的 brief。
- `rejected/`：已放弃或被替代的 brief。
- `TEMPLATE.md`：创建新 brief 的统一模板。

规则：

- 每个 UI/UX 任务必须先建 brief。
- brief 必须及时归类到 `active / accepted / rejected` 之一。
- 禁止存在未分类 brief。
