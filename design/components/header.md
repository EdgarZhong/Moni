# Header

## 角色

首页与主流程的全局顶部锚点，承接品牌识别与账本切换。

## 结构

- 左侧：`Moni` 字标。
- 右侧：账本选择器。

## 规则

- Header 始终为粘性顶部元素。
- 首页滚动与无限流水滚动时，Header 不消失。
- 首页中的账本选择器只负责切换，不承接复杂管理。
- 账本管理入口归属设置页。

## 状态

- 默认态：显示当前账本名。
- 滚动态：保持固定。
- 下方轨道吸附态：分类标签轨道吸附到 Header 下方。
